CREATE TYPE seahorse_dev.address (
    id uuid,
    addresstype text,
    isprimary text,
    address1 text,
    address2 text,
    address3 text,
    city text,
    state text,
    country text,
    postalcode text,
    status text
);
